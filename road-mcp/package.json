{
  "name": "sara-omega-road-mcp",
  "version": "0.2.0",
  "private": true,
  "description": "ROAD: headless MCP server for SARA OMEGA CHAT GPT custom. Read-only roadmap, gate-evidence, and certification tools. Never renders UI; never manufactures PASS.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/server.js",
    "test": "node --test tests/"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "express": "^4.21.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0"
  }
}
